<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Co-work Office Finder - Start</title>
    <link rel="stylesheet" href="styles/start.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">  
    <style>
    </style>
</head>
<body>
    <div class="container mt-4">
        <!-- Input and Button Row -->
        <div class="row mb-3 align-items-end">
            <div class="col-12 mb-2">
                <h6><label for="currentLocation">My Location:</label>&nbsp;<span style="color: grey; font-size: 10px;">(or other address - Funan, Singapore 117438)</span></h6>           
            </div>
            <div class="col-12 col-md-6">
                <input type="text" id="currentLocation" class="form-control" placeholder="Enter your location">
            </div>
            <div class="col-12 col-md-6 d-flex mt-2">
                <button id="showAddressBtn" class="btn btn-custom me-1" style="color: white; background-color: orangered;">Use entered Address</button>
                <button id="refreshLocationBtn" class="btn btn-blue me-1" style="color: white; background-color: blue;">Use current Location</button>
                <button id="findOfficesBtn" class="btn btn-green" style="color: white; background-color: green;">Find Offices</button>
            </div>
        </div>
       
        <!-- Pagination and Sort Toggle -->
        <div id="pagination" class="d-flex justify-content-between align-items-center mb-3" style="display: none;">
            <div class="d-flex">
                <button id="prevButton" class="btn" style="color: white; background-color: magenta; margin-right: 10px; display: none;">Prev</button>
                <button id="nextButton" class="btn" style="color: white; background-color: magenta; display: none;">Next</button>
            </div>
    
            <div id="sortToggle" class="form-check form-switch">
                <label class="form-check-label" for="sortToggleSwitch">Duration:Distance [left:right]</label>
                <input class="form-check-input" type="checkbox" id="sortToggleSwitch" checked>
            </div>
        </div>

        <!-- Office Cards Grid -->
        <div class="row">
            <div class="col-md-4 card-column" id="column1"></div>
            <div class="col-md-4 card-column" id="column2"></div>
            <div class="col-md-4 card-column" id="column3"></div>
        </div>
    
        <!-- New div for Google Map -->
        <div id="map" class="mt-4"></div>
    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="https://maps.googleapis.com/maps/api/js?key=<%= process.env.GOOGLE_MAPS_API_KEY; %>"></script>
    <script src="start.js"></script>
</body>
</html>